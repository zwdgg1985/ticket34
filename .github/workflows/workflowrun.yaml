name: workflow run
on:
  workflow_run:
    workflows: ["manual check"]
    types:
      - completed
      - requested
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
